<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dayan.food.mapper.AppUserMapper">

    <resultMap id="appUserResultMap" type="com.dayan.food.entity.po.AppUser">
        <id property="id" column="id"/>
        <result property="subjectId" column="subject_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="displayName" column="display_name"/>
        <result property="email" column="email"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="signature" column="signature"/>
        <result property="signaturePending" column="signature_pending"/>
        <result property="signatureStatus" column="signature_status" javaType="com.dayan.food.entity.enums.SignatureStatus"/>
        <result property="role" column="role" javaType="com.dayan.food.entity.enums.UserRole"/>
        <result property="active" column="active"/>
        <result property="authVersion" column="auth_version"/>
        <result property="lastFoodUploadAt" column="last_food_upload_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="userColumns">
        id, subject_id, username, password, display_name, email, avatar_url, signature, signature_pending,
        signature_status, role, active, auth_version, last_food_upload_at, created_at
    </sql>

    <select id="findByUsername" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="findByUsernameOrEmail" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        WHERE username = #{identity}
           OR email = #{identity}
        ORDER BY CASE WHEN username = #{identity} THEN 0 ELSE 1 END
        LIMIT 1
    </select>

    <select id="findByUsernameOrDisplayName" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        WHERE username = #{identity}
           OR display_name = #{identity}
        ORDER BY CASE WHEN username = #{identity} THEN 0 ELSE 1 END
        LIMIT 1
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM app_user
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        WHERE id = #{id}
    </select>

    <select id="findByUsernameForUpdate" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        WHERE username = #{username}
        FOR UPDATE
    </select>

    <select id="findByIdForUpdate" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/> FROM app_user WHERE id = #{id} FOR UPDATE
    </select>

    <update id="updateAvatarById">
        UPDATE app_user SET avatar_url = #{avatarUrl} WHERE id = #{id} AND active = TRUE
    </update>

    <!-- 公开页读侧：userColumns 之外的敏感列（如 password）不进入查询与内存。 -->
    <select id="findPublicById" resultMap="appUserResultMap">
        SELECT id, subject_id, username, display_name, avatar_url, signature, active, auth_version
        FROM app_user
        WHERE id = #{id}
    </select>

    <select id="findPage" resultMap="appUserResultMap">
        SELECT <include refid="userColumns"/>
        FROM app_user
        ORDER BY created_at DESC, id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM app_user
    </select>

    <select id="countByAvatarUrl" resultType="int">
        SELECT COUNT(*)
        FROM app_user
        WHERE avatar_url = #{avatarUrl}
    </select>

    <update id="updateAvatar">
        UPDATE app_user
        SET avatar_url = #{avatarUrl}
        WHERE username = #{username}
          AND active = TRUE
    </update>

    <update id="updateSignature">
        UPDATE app_user
        SET signature = #{signature}
        WHERE id = #{id}
    </update>

    <update id="updateDisplayName">
        UPDATE app_user
        SET display_name = #{displayName}
        WHERE id = #{id}
    </update>

    <update id="updateActive">
        UPDATE app_user
        SET active = #{active}, auth_version = auth_version + 1
        WHERE id = #{id}
    </update>

    <update id="updateRole">
        UPDATE app_user
        SET role = #{role}, auth_version = auth_version + 1
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE app_user
        SET password = #{password}, auth_version = auth_version + 1
        WHERE username = #{username}
          AND LOWER(email) = #{email}
          AND active = TRUE
    </update>
    <delete id="deleteById">
        DELETE FROM app_user
        WHERE id = #{id}
    </delete>
    <insert id="enqueueExternalDeletion">
        INSERT INTO external_identity_deletion(subject_id, target)
        VALUES(#{subjectId}, #{target})
        ON DUPLICATE KEY UPDATE status='PENDING', next_attempt_at=CURRENT_TIMESTAMP
    </insert>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO app_user (
            subject_id, username, password, display_name, email, role, active, auth_version, last_food_upload_at, created_at
        )
        VALUES (
            UUID(), #{username}, #{password}, #{displayName}, #{email}, #{role}, #{active}, 1,
            #{lastFoodUploadAt}, #{createdAt}
        )
    </insert>
</mapper>
